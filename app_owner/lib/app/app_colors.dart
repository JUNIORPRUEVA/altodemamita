import 'package:flutter/material.dart';

class AppColors {
  // Brand accent (usado con moderación)
  static const primary = Color(0xFF1A3A5C);
  static const primaryLight = Color(0xFFE8EFF6);

  // Fondos
  static const background = Color(0xFFF8FAFC);
  static const surface = Color(0xFFFFFFFF);

  // Texto
  static const textPrimary = Color(0xFF1A2A3A);
  static const textSecondary = Color(0xFF8B9AAB);
  static const textMuted = Color(0xFFB0BCC8);

  // Bordes
  static const border = Color(0xFFE8EDF2);
  static const borderLight = Color(0xFFF0F3F7);

  // Acentos suaves para tarjetas
  static const accentBlue = Color(0xFF2B5C88);
  static const accentGreen = Color(0xFF2E8B68);
  static const accentAmber = Color(0xFFB9791E);
  static const accentRose = Color(0xFF9B516B);

  // Sombra
  static const shadow = Color(0x08000000);
}
