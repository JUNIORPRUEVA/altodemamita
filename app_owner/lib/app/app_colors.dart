import 'package:flutter/material.dart';

class AppColors {
  // Identidad compartida con la app local.
  static const primary = Color(0xFF16324F);
  static const primaryLight = Color(0xFFE9EEF4);
  static const secondary = Color(0xFF2B6B4A);
  static const secondaryLight = Color(0xFFE8F1EC);

  // Fondos
  static const background = Color(0xFFF0F3F8);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceWarm = Color(0xFFF8FAFC);

  // Texto
  static const textPrimary = Color(0xFF1D2733);
  static const textSecondary = Color(0xFF6A7684);
  static const textMuted = Color(0xFF9099A4);

  // Bordes
  static const border = Color(0xFFD7DFEA);
  static const borderLight = Color(0xFFE4EAF2);

  // Acentos suaves para módulos secundarios.
  static const accentBlue = primary;
  static const accentGreen = secondary;
  static const accentAmber = Color(0xFFB9791E);
  static const accentRose = Color(0xFF9B516B);

  // Sombra
  static const shadow = Color(0x08000000);
}
