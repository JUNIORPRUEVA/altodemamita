import 'package:flutter/material.dart';

// Placeholder root app. We'll refactor OwnerApp into here in next step.
class AppOwnerApp extends StatelessWidget {
  const AppOwnerApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
